import { config, defineConfig } from "next/config";
import type { App } from "next/types";
import type { NextConfig } from "next/types";

const nextConfig: NextConfig = {};

export default defineConfig(nextConfig);
